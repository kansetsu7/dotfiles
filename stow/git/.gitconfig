[alias]
  cl = clone
  st = status
  ci = commit
  cia = commit -a
  co = checkout
  br = branch
  bra = branch -a
  df = diff
  sh = show
  re = reset
  me = merge
  cf = config
  sa = stash
  lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)%Creset' --abbrev-commit
  lgp = log -p --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)%Creset' --abbrev-commit
	conflicts = diff --name-only --diff-filter=U

[gui]
  fontdiff = -family Courier -size 12 -weight normal -slant roman -underline 0 -overstrike 0

[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto

[core]
  editor = nvim
  pager = diff-so-fancy | less --tabs=4 -RFX
  excludesfile = ./.gitignore_global

[color]
  ui = true

[color "diff-highlight"]
  oldNormal = red bold
  oldHighlight = red bold 52
  newNormal = green bold
  newHighlight = green bold 22

[color "diff"]
    meta = 11
    frag = magenta bold
    func = 146 bold
    commit = yellow bold
    old = red bold
    new = green bold
    whitespace = red reverse

[diff-so-fancy]
    markEmptyLines = false

[tig]
  show-rev-graph = yes
  line-graphics = no

[branch]
  autosetuprebase = always

[push]
  default = simple

; [pager]
;   diff = diff-highlight | less
;   log = diff-highlight | less
;   show = diff-highlight | less

[interactive]
  diffFilter = diff-so-fancy --patch
[user]
	name = Andre Kuo
	email = andre.kuo@abagile.com
[core]
	editor = nvim
